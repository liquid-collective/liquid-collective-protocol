{
  "files": [
    "certora/harness/OperatorsRegistryV1Harness.sol"
  ],
  "verify": "OperatorsRegistryV1Harness:certora/specs/parametricInvariant.spec",
  // The method `addValidators` needs 4 iterations, since:
  // - `ValidatorKeys.PUBLIC_KEY_LENGTH + ValidatorKeys.SIGNATURE_LENGTH = 144`
  // - In `LibBytes.sol:slice` line 55 we loop 144 // 32 = 4 times
  "loop_iter": "2",
  "optimistic_loop": true,
  "optimistic_hashing": true,
  "rule_sanity": "basic",
  "packages": ["openzeppelin-contracts=lib/openzeppelin-contracts"],
  "prover_args": [
    "-optimisticFallback true",
    "-enableCopyLoopRewrites true"
    ],
  "msg": "Invariants and parametric rules",
  "server": "production",
  "smt_timeout": "5000"
}
