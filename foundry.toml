[profile.default]
solc_version = "0.8.34"
optimizer = true
optimizer_runs = 100

[rpc_endpoints]
mainnet = "${MAINNET_FORK_RPC_URL}"

[fuzz]
runs = 1500

[invariant]
runs = 128
depth = 32
fail_on_revert = false