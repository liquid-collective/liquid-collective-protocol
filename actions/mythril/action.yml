
# action.yml
name: 'Mythril'
description: 'Run Mythril'
inputs:
  contract:
    description: 'Contract to test'
    required: true
runs:
  using: 'docker'
  image: 'mythril/myth'
  args:
    - 'analyze'
    - ${{ inputs.contract }}
    - '--execution-timeout'
    - '300'
    - '-o'
    - 'markdown'